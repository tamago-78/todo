# 個人開発のやり方ガイド

このリポジトリ（Todoアプリ）を題材に、個人開発をどう進めればいいかをまとめたガイドです。
すでにこのリポジトリには React + TypeScript + Vite + テスト + Linter という個人開発に十分な土台が揃っているので、これを活かす前提で書いています。

## 全体の流れ

個人開発は大きくこの5ステップの繰り返しです。

1. **作るものを決める**（小さく）
2. **動く最小版（MVP）を作る**
3. **自分で使ってみる**
4. **公開する**
5. **フィードバックをもとに改善する**

一番よくある失敗は「1で大きくしすぎて2が終わらない」ことです。最初の目標は「1〜2週間で自分が毎日使えるもの」に絞りましょう。

## 1. 作るものを決める

- **自分が欲しいものを作る**のが鉄則です。ユーザー第1号が自分なら、モチベーションも続き、フィードバックも即座に得られます。
- アイデアは「不便に感じた瞬間」をメモしておくと貯まります。
- 決めたら、機能を「必須 / あとで / やらない」の3つに分類し、必須だけで最初のバージョンを定義します。

例：このTodoアプリなら「タスクの追加・完了・削除ができる」が必須で、QRコード共有やPWA化は「あとで」に入る機能です。

## 2. 開発環境と日々の進め方

### このリポジトリでの基本コマンド

```bash
npm install        # 初回のみ：依存パッケージのインストール
npm run dev        # 開発サーバー起動（ブラウザで即確認できる）
npm test           # テスト実行（vitest）
npm run build      # 本番用ビルド
```

### Gitの使い方（1人でもブランチを切る）

1人開発でも `master` に直接コミットせず、変更ごとにブランチを切るのがおすすめです。

```bash
git switch -c feature/add-due-date   # 作業ブランチを作る
# ...開発...
git add -A && git commit -m "期限日を追加できるようにする"
git push -u origin feature/add-due-date
```

- コミットは「1つの変更 = 1コミット」を意識し、メッセージは日本語でOK。「何をしたか」より「なぜしたか」が伝わると後で助かります。
- GitHub上でPull Requestを作ってからマージすると、変更履歴が読みやすくなります（このリポジトリのPR #1 がまさにその例です）。

### AI（Claude Codeなど）との分担

- **AIに任せると速いこと**：ボイラープレート作成、テスト追加、リファクタ、エラーの原因調査、ライブラリの使い方調査。
- **自分でやるべきこと**：何を作るかの判断、実際に触っての使い心地チェック、公開してよい内容かの最終確認。
- AIが書いたコードも `npm test` と `npm run dev` での動作確認は必ず自分で行いましょう。

## 3. 品質を保つ最低限のライン

このリポジトリにはすでに揃っていますが、個人開発で持っておきたいのはこの3点です。

| 仕組み | このリポジトリでの実体 | 役割 |
|---|---|---|
| Linter | ESLint（`.eslistrc.json`） | バグの芽を機械的に検出 |
| Formatter | Prettier（`.prettierrc.json`） | 整形を自動化して迷いをなくす |
| テスト | Vitest（`src/__tests__/`） | 壊したことにすぐ気づける |

テストは全部に書く必要はありません。`src/lib/isTodos.ts` のような**ロジックが集中している関数**にだけ書けば十分効果があります。

## 4. 公開する

Webアプリの公開先は無料枠で十分です。

- **GitHub Pages**：このリポジトリはすでに `gh-pages` パッケージと `static.yml` ワークフローがあるので、一番手軽です。
- **Vercel / Netlify / Cloudflare Pages**：GitHubリポジトリを連携するだけで、pushのたびに自動デプロイされます。独自ドメインやプレビューURLが欲しくなったらこちら。

### 公開前チェックリスト

- [ ] APIキー・パスワード等の秘密情報がコードやコミット履歴に含まれていない（`.gitignore` の確認、`.env` はコミットしない）
- [ ] 個人情報や社内情報が含まれていない
- [ ] READMEに「何のアプリか・どう使うか」を書いた
- [ ] スマホでも表示を確認した

**注意**：公開は取り消しにくい操作です。リポジトリ自体をpublicにする場合は、履歴全体に見られて困るものがないか公開前に必ず確認しましょう。

## 5. 続けるコツ

- **小さく完成させる**：未完成の大作より、動く小品。リリースすること自体が最大の学びです。
- **毎回「次にやること」をIssueに残して終える**：再開時のハードルが下がります。GitHubのIssueがそのままTODOリストになります。
- **飽きたら捨てていい**：個人開発は学びが目的でも十分。リポジトリが残っていればポートフォリオになります。

## 次のステップの例（このTodoアプリの場合）

- タスクに期限や優先度を追加する
- ドラッグ＆ドロップで並び替え
- ダークモード対応
- GitHub Pagesで公開して友人に使ってもらう

まずは1つ選んでブランチを切り、小さく作って動かすところから始めましょう。

---

# モバイルアプリ編（iOS / Android 両対応）

## 結論：Expo（React Native）がおすすめ

理由はシンプルで、**このリポジトリで使っているReactとTypeScriptの知識がそのまま使える**からです。1つのコードでiOSとAndroidの両方のアプリが作れます。

| 選択肢 | 言語 | 向いている人 |
|---|---|---|
| **Expo (React Native)** | TypeScript / React | React経験者（＝今のあなた） |
| Flutter | Dart（新しく学ぶ） | ゼロから学び直してもいい人 |
| Capacitor | 今のWebアプリをそのまま包む | 既存Webアプリを最速でアプリ化したい人 |

補足：今のTodoアプリはPWA対応済みなので、実は「スマホのホーム画面に追加」すれば今でもアプリ風に使えます。ストア配布が目的でなければ、まずこれで済むこともあります。

## Expoの始め方

Macは不要です。手元のPCと、実機のスマホ（iPhoneでもAndroidでも）があれば始められます。

```bash
npx create-expo-app@latest my-app --template blank-typescript
cd my-app
npx expo start
```

1. スマホに **Expo Go** アプリ（App Store / Google Play で無料）を入れる
2. `npx expo start` で表示されるQRコードをスマホで読み取る
3. **自分のスマホ上でアプリが即座に動く**。コードを保存すると自動で反映される

この体験の速さがExpo最大の魅力で、Xcode や Android Studio のセットアップなしで開発を始められます。

## Webとの違いで最初に覚えること

- `<div>` → `<View>`、`<p>`や`<span>` → `<Text>`、`<button>` → `<Pressable>`
- CSSファイルの代わりに `StyleSheet.create({...})`（書き方はほぼCSS）
- 画面遷移は **Expo Router**（ファイルベースでNext.jsに似ている）
- データ保存は `localforage` の代わりに **AsyncStorage**

Todoアプリをもう一度Expoで作り直すのが、最高の練習課題になります。ロジック（`isTodos.ts` のような部分）はほぼコピーで動きます。

## 配布方法（段階別）

| 段階 | 方法 | 費用 |
|---|---|---|
| 自分で使う | Expo Go で起動、または development build | 無料 |
| 友人に試してもらう | **EAS Build**（Expoのクラウドビルド）+ Android は APK 直接配布、iOS は TestFlight | 無料枠あり |
| ストアで公開 | Google Play：初回 $25（買い切り、約4,000円）/ App Store：**Apple Developer Program 年間 $99（日本からは年間12,980円で請求）** | 左記 |

- iOSのビルドは本来Macが必要ですが、**EAS Buildを使えばクラウド上でビルドされるのでMacなしでもストア提出まで可能**です。
- ストア公開は審査があります（特にAppleは1〜3日程度）。最初は「自分とTestFlightの友人まで」で十分です。

## おすすめの進め方

1. `create-expo-app` で空プロジェクトを作り、Expo Goで動かしてみる（30分）
2. このTodoアプリをExpoで再実装する（数日〜1週間）
3. AsyncStorageで保存できるようにして、自分のスマホで毎日使う
4. 使っていて欲しくなった機能（通知、ウィジェットなど）を足す
5. TestFlight / APKで友人に配ってみる

Webの時と同じく「小さく作って、まず自分で使う」が鉄則です。

---

# マネタイズ編（年間費をペイする）

目標を明確にすると、**年間コストは約13,000円（Apple 12,980円 + Google初回のみ約4,000円）＝ 月あたり約1,100円**。これを超えれば黒字です。

## 収益化の選択肢と「ペイするライン」

| 方法 | 仕組み | 月1,100円に必要な規模の目安 |
|---|---|---|
| **広告（AdMob）** | 無料アプリに広告表示 | 1日100人前後が使うアプリ（表示回数による） |
| **買い切り** | 有料アプリ（例：300円） | 年間50〜60ダウンロード |
| **アプリ内課金** | 基本無料＋プレミアム機能（例：500円） | 年間30〜40人が課金 |
| **サブスク** | 月額課金（例：100〜300円/月） | 常時5〜10人の課金者 |

- ストアの手数料は売上の15%（年間収益100万円以下の個人はApple/Googleとも15%枠が使える。Appleは「App Store Small Business Program」への申請が必要）。
- 個人開発の定番は「**無料＋広告、買い切りで広告解除**」のハイブリッド。1つのアプリで両方の収益が入ります。

## ペイするための戦略

1. **ニッチを狙う**：「Todoアプリ」のような激戦区ではなく、「特定の趣味・職業・状況」に絞る（例：釣り記録、シフト勤務者向け給料計算、部活のスコア記録）。検索されるが競合が少ない領域が個人開発の勝ち筋です。
2. **ストア検索（ASO）が集客のほぼすべて**：個人開発アプリのダウンロードの大半はストア内検索から。アプリ名に「何をするアプリか」を含める（例：「釣果ノート - 釣り記録」）。スクリーンショット1枚目が実質の広告です。
3. **最初から収益化を入れておく**：後から広告を足すより、リリース時点でAdMob1枠だけでも入れておく。Expoなら `react-native-google-mobile-ads` で実装できます。
4. **1本で当てようとしない**：個人開発は「小さいアプリを複数」が定石。3本目くらいで感覚がつかめて、当たった1本が他を養います。
5. **維持費を増やさない**：サーバーが必要な設計にすると収益より先にコストが伸びます。端末内保存（AsyncStorage）で完結する設計なら、コストは年会費だけです。

## 現実的な期待値

- 1本目のアプリでペイできる人は少数派です。1本目は「作り方と出し方を学ぶ授業料」と割り切るのが健全。
- 一方で、ニッチに刺さった個人開発アプリが月数千〜数万円を安定して稼ぐ例は珍しくありません。年13,000円は「小さなヒット1本」で十分回収できる金額です。
- リスクを抑えたいなら、**初年度はGoogle Play（買い切り約4,000円）だけで出して反応を見て、手応えがあったらApple（年12,980円）に広げる**という順番もあります。

## 収益が出たら

- 広告・課金収入は雑所得（または事業所得）。**給与所得者は年間の雑所得等が20万円を超えたら確定申告が必要**です。超えるまでは基本的に申告不要（住民税の申告は別途必要な場合あり）。

---

# 無料アプリのマネタイズ実装（AdMob＋広告解除）

## お金が入る仕組み

**Google AdMob**（無料）にアプリを登録すると、アプリ内に広告枠を置けます。広告が表示・タップされるたびに収益が積み上がり、**合計8,000円に達すると翌月に銀行口座へ振り込まれる**仕組みです。収益の目安は「表示1,000回あたりいくら（eCPM）」で考えます。

## 広告の種類と使い分け

| 種類 | 見た目 | 収益性の目安 | 使いどころ |
|---|---|---|---|
| バナー | 画面下部に常時表示 | 低（eCPM 数十〜百円台） | まず最初に入れる定番 |
| インタースティシャル | 全画面（数秒で閉じられる） | 中〜高 | 操作の区切り（保存完了後など）。頻度は控えめに |
| リワード | 動画を見ると特典がもらえる | 高（eCPM 千円台〜） | ユーザーが自分で選んで見るので嫌われにくい |

定番構成は「**バナー常時＋区切りでたまにインタースティシャル**」。広告を増やしすぎるとレビューが荒れてダウンロードが減り、逆効果になります。

### バナーだけの場合の収益試算

収益 ≒ 1日のユーザー数（DAU）× 1人あたりの広告表示回数 × eCPM ÷ 1000。
バナーは約60秒ごとに切り替わるので「1日5分使うアプリなら1人あたり約5回表示」と見積もれます。日本のバナーeCPMをやや保守的に50円と置くと：

| 1日のユーザー数 | 月間表示回数 | 月収の目安 |
|---|---|---|
| 100人 | 約15,000回 | **約750円** |
| 200人 | 約30,000回 | 約1,500円（年会費ペイライン） |
| 1,000人 | 約150,000回 | 約7,500円 |
| 10,000人 | 約1,500,000回 | 約75,000円 |

- eCPMは実際には10〜150円程度と振れ幅が大きく、ジャンル・季節（12月は高い、1月は安い）・広告の位置で変わります。
- **バナーだけで年会費をペイするなら「毎日200人が5分使うアプリ」が目安**。滞在時間の長いアプリ（タイマー、メモ、ゲーム）はバナーと相性が良く、一瞬で用が済むアプリは表示回数が稼げないため不利です。

## 実装手順（Expoの場合）

1. **AdMobアカウント作成**（無料）→ アプリを登録し「広告ユニットID」を発行
2. ライブラリ導入：`react-native-google-mobile-ads`（Expoのconfig pluginに対応）
3. **開発中はGoogle提供のテスト用IDを使う**（自分のアプリの本物の広告を自分でタップするとアカウント停止のリスクがあるため。これはリリース後も同様に厳禁）
4. Expo Goでは広告SDKは動かないため、`eas build` で **development build** を作って実機確認
5. リリースビルドで本番IDに切り替えて申請

## 広告解除課金をセットにする

「買い切り300〜500円で広告を消せる」アプリ内課金を足すのが定番ハイブリッドです。広告が嫌な人からは課金で、それ以外からは広告で収益が入ります。実装は **RevenueCat**（個人規模なら無料枠で十分）を使うと、Apple/Google両方の課金処理をまとめて扱えて簡単です。

## 注意点まとめ

- 自分の広告を自分でクリックしない（家族・友人に頼むのもNG。検知されると停止）
- 開発中は必ずテストIDを使う
- 子ども向けアプリ扱いになると出せる広告が制限されるので、ストア申請時の対象年齢設定に注意
- AdMobの収益はアプリ規模に比例します。まず「使われるアプリ」を作るのが先で、広告はその上に乗るもの
