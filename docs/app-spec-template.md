# アプリ仕様書：（アプリ名 or 仮称）

> 個人開発用の軽量テンプレート。全部埋めなくてOK。
> 「1. 概要」「3. 機能」「4. 画面」の3つが埋まれば開発を始められます。
> 迷ったら空欄のまま「9. 未決事項」に書いて先に進むこと。

## 1. 概要

- **一言で言うと**：（例：釣った魚を3タップで記録できるアプリ）
- **誰の、どんな不便を解決する？**：（例：釣りの最中は手が濡れていて長文メモが無理）
- **既存アプリとの違い**：（例：SNS機能なし。記録特化で起動が速い）

## 2. ユーザーと利用シーン

- **想定ユーザー**：（例：週末に堤防釣りをする人）
- **使うタイミング**：（例：釣れた直後、帰宅後の振り返り）
- **1日に開く回数 / 1回の滞在時間**：（例：釣行日に5〜10回 × 30秒）
  ※広告収益はここで決まる。「毎日 or 高頻度で開く理由」があるか自問する

## 3. 機能一覧

### 必須（これだけでv1リリース）
- [ ] （例：記録の追加：魚種・サイズ・写真）
- [ ] （例：記録の一覧表示・削除）

### あとで（v2以降）
- （例：釣り場の地図表示）

### やらない（明示しておくと迷いが消える）
- （例：アカウント登録、SNS共有、ランキング）

## 4. 画面一覧と遷移

```
[ホーム(一覧)] ──「＋」→ [記録作成]
      │
      └─ 行タップ → [記録詳細] ─ 編集 → [記録作成(編集モード)]
      └─ 設定アイコン → [設定]
```

各画面にあるもの：
| 画面 | 主な要素 |
|---|---|
| ホーム | 記録リスト、＋ボタン、（バナー広告） |
| 記録作成 | 入力フォーム、保存ボタン |
| 設定 | 広告解除購入、データ削除、バージョン表示 |

## 5. データ

保存するもの（端末内・AsyncStorage想定）：

| 項目 | 型 | 例 |
|---|---|---|
| id | string | "uuid" |
| （例：魚種） | string | "アジ" |
| createdAt | number | 1722650000000 |

- サーバーは使わない（維持費ゼロ方針）。使うなら理由を書く：
- 個人情報は保存しない。写真等を扱う場合の注意：

## 6. マネタイズ

- [ ] バナー広告（位置：例 ホーム画面下部固定）
- [ ] インタースティシャル（タイミング：例 記録保存5回ごと）
- [ ] 広告解除の買い切り課金（価格：例 400円）
- [ ] なし（まず使われることを優先）

## 7. 技術構成

- フレームワーク：Expo (React Native) + TypeScript
- 画面遷移：Expo Router
- データ保存：AsyncStorage
- 広告：react-native-google-mobile-ads（v1では入れない場合は消す）
- その他ライブラリ：

## 8. リリース計画

- **v1の完成条件**：（例：自分が実際の釣行で使えた）
- 配布：まず自分（Expo Go / dev build）→ 友人（APK / TestFlight）→ ストア
- 対応OS：iOS / Android 両方
- 言語：日本語のみ

## 9. 未決事項・メモ

- （例：写真は保存する？端末容量との兼ね合いは？）
